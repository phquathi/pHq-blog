---

import { profileConfig } from "../config";
import { url } from "../utils/url-utils";

const currentYear = new Date().getFullYear();
---

<!--<div class="border-t border-[var(&#45;&#45;primary)] mx-16 border-dashed py-8 max-w-[var(&#45;&#45;page-width)] flex flex-col items-center justify-center px-6">-->
<div class="transition border-t border-black/10 dark:border-white/15 my-10 border-dashed mx-32"></div>
<!--<div class="transition bg-[oklch(92%_0.01_var(&#45;&#45;hue))] dark:bg-black rounded-2xl py-8 mt-4 mb-8 flex flex-col items-center justify-center px-6">-->
<div class="transition border-dashed border-[oklch(85%_0.01_var(--hue))] dark:border-white/15 rounded-2xl mb-12 flex flex-col items-center justify-center px-6">
    <div class="transition text-50 text-sm text-center">
        &copy; <span id="copyright-year">{currentYear}</span> {profileConfig.name}. All Rights Reserved. /
        <a class="transition link text-[var(--primary)] font-medium" target="_blank" href={url('rss.xml')}>RSS</a> /
        <a class="transition link text-[var(--primary)] font-medium" target="_blank" href={url('sitemap-index.xml')}>Sitemap</a><br>
        Powered by
        <a class="transition link text-[var(--primary)] font-medium" target="_blank" href="https://astro.build">Astro</a> &
        <a class="transition link text-[var(--primary)] font-medium" target="_blank" href="https://github.com/saicaca/fuwari">Fuwari</a>
    </div>
    <!-- 不蒜子访客统计（沿用旧站统计服务，数据延续） -->
    <div class="transition text-50 text-sm text-center mt-2" data-pagefind-ignore>
        <span id="busuanzi_container_site_pv">本站总访问量 <span id="busuanzi_value_site_pv"></span> 次</span>
        <span id="busuanzi_container_site_uv" class="ml-1.5">访客数 <span id="busuanzi_value_site_uv"></span> 人</span>
    </div>
</div>

<script>
    // 不蒜子访问统计（https://busuanzi.ibruce.info）。
    // 注意：官方脚本文件名是 busuanzi.pure.mini.js（不是 min.js，写错会 404 导致统计永远不显示）。
    // 容器默认可见，脚本加载成功后 busuanzi 自动填充数字；
    // 多 CDN 兜底，全部失败时才隐藏统计区；
    // swup 软导航会重建页面内容，需要在 content:replace 后重新加载以刷新文章阅读数。
    const busuanziSources = [
        "https://busuanzi.ibruce.info/busuanzi/2.3/busuanzi.pure.mini.js",
        "https://cdn.bootcdn.net/ajax/libs/busuanzi/2.3.0/bsz.pure.mini.js",
        "https://cdn.jsdelivr.net/npm/busuanzi@2.3.0/bsz.pure.mini.js",
    ];
    function loadBusuanzi(index = 0) {
        if (index >= busuanziSources.length) return;
        const script = document.createElement("script");
        script.src = busuanziSources[index];
        script.async = true;
        script.onerror = () => loadBusuanzi(index + 1);
        document.head.appendChild(script);
    }
    loadBusuanzi();
    function setupBusuanziSwupHook() {
        if (window.swup) {
            window.swup.hooks.on("content:replace", () => loadBusuanzi());
        } else {
            document.addEventListener("swup:enable", setupBusuanziSwupHook, { once: true });
        }
    }
    setupBusuanziSwupHook();
</script>