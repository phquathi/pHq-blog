---
title: "acwing暑假每日一题（2022/7/5）"
published: 2022-07-05
description: "acwing 3564.日期类3564. 日期类 - AcWing题库 很简单的一道模拟题，博主直接暴力一遍过（"
tags: []
category: ""
draft: false
legacyPath: "2022/07/05/acwing暑假每日一题（2022-7-5）"
---

## acwing 3564.日期类

[3564. 日期类 - AcWing题库](https://www.acwing.com/problem/content/3567/)

很简单的一道模拟题，博主直接暴力一遍过（

```c++
#include<bits/stdc++.h>
using namespace std ;

#define ll long long
#define ull unsigned long long
#define pb emplace_back
#define gtc cin.get()
#define endl (char)10

const int N = 1e3 + 10 ;
int dx[] = { -1, 0, 1, 0 };
int dy[] = { 0, -1, 0, 1 };
int f[N][N] ;
int w[N] , g[N] , val[N] ;

int run( int x ){
    if ( x % 400 == 0 || (x % 4 == 0 && x % 100 != 0 ) ) return 1 ;
    else return 0 ;
}

int main () {
   int n ;
    cin >> n ;
    while( n -- ){
        int y , m , d ;
        scanf( "%d %d %d" , &y , &m , &d);
        if ( run(y) ){
            d ++ ;
            if ( m == 1 || m == 3 || m == 5 || m == 7 || m == 8 || m == 10 || m == 12 ) {
                if (d > 31) {
                    d = 1;
                    m++;
                    if (m > 12) {
                        m = 1;
                        y++;
                    }
                }
            }
            else if ( m == 4 || m == 6 || m == 9 || m == 11 ){
                if (d > 30) {
                    d = 1;
                    m++;
                }
            }
            else if ( m == 2 ){
                if (d > 29) {
                    d = 1;
                    m++;
                }
            }
        }
        else{
            d ++ ;
            if ( m == 1 || m == 3 || m == 5 || m == 7 || m == 8 || m == 10 || m == 12 ) {
                if (d > 31) {
                    d = 1;
                    m++;
                    if (m > 12) {
                        m = 1;
                        y++;
                    }
                }
            }
            else if ( m == 4 || m == 6 || m == 9 || m == 11 ){
                if (d > 30) {
                    d = 1;
                    m++;
                }
            }
            else if ( m == 2 ){
                if (d > 28) {
                    d = 1;
                    m++;
                }
            }
        }
        printf("%d-%02d-%02d\n" , y , m , d ) ;
    }
   return 0 ;
   }
```

可过了之后博主看到题面有一行小字

“**输入日期保证合法且不会出现闰年**”

不会出现闰年我还加判闰年…服了

```c++
 int run( int x ){
if ( x % 400 == 0 || (x % 4 == 0 && x % 100 != 0 ) ) return 1 ;
    else return 0 ;
}
```

所以这告诉我们读题不要瞎
