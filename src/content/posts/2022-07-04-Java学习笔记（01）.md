---
title: "Java学习笔记（01）"
published: 2022-07-04
description: "java学习之GUI标签（JLable）JLabel 1new Jlabel(\"XXX\"); 图标Icon"
tags: []
category: ""
draft: false
legacyPath: "2022/07/04/Java学习笔记（01）"
---

## java学习之GUI

## 标签（JLable）

JLabel

```java
new Jlabel("XXX");
```

图标Icon

```java
package com.SwingStudy;

import javax.swing.*;
import java.awt.*;

/**
 手绘一个图标
 */
public class Demo03 extends JFrame implements Icon {

    private int width;
    private int height;

    public Demo03(){}

    public Demo03(int width , int height ){
        this.width = width;
        this.height = height;
    }

    public void init(){
       Demo03 demo03 = new Demo03( 15 ,15);
       JLabel jLabel = new JLabel("icontest" , demo03 , SwingConstants.CENTER) ;
       Container container = getContentPane();
       container.add(jLabel);

       this.setVisible(true);
       this.setDefaultCloseOperation(WindowConstants.HIDE_ON_CLOSE);
    }
    public static void main(String[] args) {
        new Demo03().init();
    }

    @Override
    public void paintIcon(Component c, Graphics g, int x, int y) {
        g.fillOval( x, y, width , height );
    }

    @Override
    public int getIconWidth() {
        return this.width;
    }

    @Override
    public int getIconHeight() {
        return this.height;
    }
}
```

图片Icon

```java
package com.SwingStudy;

import javax.swing.*;
import java.awt.*;
import java.net.URL;

/**
 插入图片图标
 */
public class Demo04 extends JFrame {
    public Demo04(){
        //获取图片地址
        JLabel label = new JLabel("apexLegends");
        URL url = Demo04.class.getResource("apex.gif");//获取当前类同级目录下资源
        ImageIcon imageIcon = new ImageIcon(url);
        label.setIcon(imageIcon);
        label.setHorizontalAlignment(SwingConstants.CENTER);

        Container container = getContentPane();
        container.add(label);

        setVisible(true);
        setDefaultCloseOperation(WindowConstants.EXIT_ON_CLOSE);
        setBounds(100 , 100 , 200 ,200);
    }

    public static void main(String[] args) {
        new Demo04();
    }
}
```

注意 `类名.class.getResource("URL")` 的使用，获取当前同级目录下的文件，需填入文件地址
